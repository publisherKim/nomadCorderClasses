#Project Setup- Git & installation
```
  setting
    1. make nuber-server folder
    2. yarn init
    3. yarn add typescript ts-node nodemon --dev
    4. make src
    5. make tsconfig.json
    6. make tslint.json
    7. yarn add @types/node --dev
    8. package.json
      "scripts": {
        "dev": "cd src && nodemon --exec ts-node index.ts"
      }
    
    cf: https://github.com/nomadcoders/nuber-server
```